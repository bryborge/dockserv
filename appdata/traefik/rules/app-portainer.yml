http:
  routers:
    portainer-rtr:
      entryPoints:
        - https
      middlewares:
        - chain-authelia
      rule: "Host(`portainer.{{ env "DOMAIN_NAME" }}`)"
      service: portainer-svc
      tls:
  services:
    portainer-svc:
      loadBalancer:
        servers:
          - port: "9000"
